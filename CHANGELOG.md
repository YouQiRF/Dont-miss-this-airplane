# 修改紀錄 / 交接文件

> 這份文件的目的：**任何時間打開專案都能立刻接手**。
> 上半部是「現在是什麼狀態」,下半部是「為什麼變成這樣」。
> 每次改動請往 `## 版本歷程` 最上面加一段。

專案：`C:\Users\youqichang\avia-cocos`
引擎：Cocos Creator 3.8.6（`package.json` 的 `creator.version`）
最後更新：2026-08-19

---

## 現在的狀態

**可以直接跑。** Dashboard 加入資料夾 → 開 `assets/scenes/game.scene` → 預覽 ▶

| 項目 | 狀態 |
|---|---|
| 演算法（`AviaPath.ts`） | 完成,8800 局 + 3000 局落海驗證 0 失敗 |
| 畫面表演（`AviaView.ts`） | 完成,語法/模組解析通過 |
| 預設美術（`AviaArt.ts`） | 完成,六種物件都能用 Prefab 整個換掉 |
| 音效層（`AviaAudio.ts`） | **線接好了,音檔還沒有**。19 格全空 = 全程靜音,不影響任何功能 |
| 組件與 UI（`AviaGame.ts`） | 完成,11 組 / 151 個 Inspector 參數全開 |
| 場景（`game.scene`） | 已建好,**60 個版面 GameObject 都在場景上**,位置直接在編輯器拖 |
| 下注系統 | 完成（可自訂下注額清單、餘額、幣別） |
| 驗證工具（`tools/verify.ts`） | 完成,`node tools/verify.ts` 直接跑 |

### 已驗證
- 演出結果精確等於目標倍數（8800 局,0 失敗,0 退回近似）
- 命中物件確實落在航線上
- 誘餌淨空 66.7px > 碰撞半徑 46 → **碰不到是硬保證**
- 航線有限、有終點、在上限之內（13–299 tick）
- 每一階高度差都等於同一常數
- 飛行途中不觸水;墜海位置分佈不集中在最後一格
- 五支腳本語法/模組解析通過
- 離線版 UI 用瀏覽器實跑（下注步進器循環、速度選單、自訂局數上限）

### 尚未驗證（需要你開編輯器）
- **實際渲染結果** —— 我無法啟動 Cocos 編輯器。場景 JSON 結構、uuid 對應、
  組件掛載都用程式核對過了,但畫面實際長相（版面、顏色、按鈕位置、觸控是否正常）
  要你開起來看。
- 觸控事件在你的 Cocos 版本上的行為
- 手機解析度下的 UI 排版

### 已知取捨
- `maxAlt` 設非 0 時,碰到高度上限的那幾階會變平 → 不再等高。預設 0（不封頂）。
- 誘餌只做視覺,不參與碰撞判定。真正的碰撞完全由腳本決定。
- `rocketDivisor` 改成非 2 的值會讓部分倍數湊不出來,系統自動退回近似值並在 Console 警告。

---

## 檔案地圖

```
avia-cocos/
├ assets/scenes/game.scene          場景（Canvas → Camera + GameRoot）
├ assets/prefabs/                   六個預設物件 Prefab（內容 = AviaArt 的預設美術）
├ assets/scripts/
│   ├ AviaPath.ts     ← 演算法。改玩法規則來這裡
│   ├ AviaView.ts     ← 畫面。改鏡頭/特效/表演節奏來這裡
│   ├ AviaArt.ts      ← 預設美術。改物件長相來這裡
│   ├ AviaAudio.ts    ← 音效層。改「什麼時候響」來這裡（音檔掛 Inspector ⑩）
│   ├ AviaUiArt.ts    ← UI 外觀 + 編輯器預覽（executeInEditMode）
│   ├ AviaDefaults.ts ← 自動產生的預設值表,別手改
│   └ AviaGame.ts     ← Inspector 參數 + 下注 + 局流程
├ tools/verify.ts                   離線驗證（不需要 Cocos）
├ tools/genlayout.js                把版面節點樹寫進場景 / --check 對帳
├ tools/gendefaults.js              重生 AviaDefaults.ts 與 DEFAULTS.md
├ DEFAULTS.md                       全部 151 個參數的預設值表
├ README.md                         設計說明與參數對照表
└ CHANGELOG.md                      這份文件
```

**改參數不用動程式** —— 全部在 `AviaGame` 的 Inspector。
`AviaGame.pushConfig()` 負責把 Inspector 的值推進演算法層,每次 `spin()` 都會重跑一次,
所以執行期改 Inspector 也會即時生效。

**加新參數的三個步驟**（三處要同步,漏掉會用到舊預設值）：
1. `AviaPath.ts` 的 `PHYS` / `SYM` / `STYLE_RANGE` 加常數
2. `AviaGame.ts` 加 `@property` + 在 `pushConfig()` 裡對應
3. 跑 `node tools/gendefaults.js` 同步預設值表與 `DEFAULTS.md`

**加一顆音效的三個步驟**：
1. `AviaAudio.ts` 的 `SfxKey` 加一個 key,順便加進 `SFX_KEYS`（開場的已掛載統計會用到）
2. `AviaGame.ts` 加一格 `@property({ type: AudioClip, group: G_SFX })`,
   並在 `audioConfig()` 的 `sfx` 裡對應
3. 在 `AviaView.onBeat()`（或動作發生的地方）呼叫 `this.sfx.play('新key')`

---

## 重新生成場景

場景檔是用腳本產生的（為了讓組件的 `__type__` 壓縮 uuid 與 `.meta` 的 uuid 對得上）。
產生器在暫存區,**每跑一次會換一組新 uuid**（整組同時換,所以是一致的）。

一般情況下**不需要重生**。只有這兩種情況才需要：
- 新增 `@property` 且想讓場景帶預設值（不重生的話會用 class 預設值,其實沒差）
- meta 或場景檔壞掉

如果要重生,產生器的核心是：uuid v4 → Cocos 壓縮 uuid（前 5 個 hex 原樣 +
其餘每 3 個 hex 轉成 2 個 base64 字元,共 23 字元）→ 寫入 `.meta` 與場景的 `__type__`。

**更省事的作法**：直接在 Cocos 編輯器裡調 Inspector 然後存檔,編輯器會自己寫回場景。

---

## 版本對照表（git tag）

每一版都打了 tag,指向那一版**收尾的那個 commit**。

| tag | 日期 | 一句話 |
|---|---|---|
| — | 2026-08-10 | v0.1 物理積分版。**沒有 tag** —— 那時候還沒有 git,而且已經整個淘汰 |
| `v0.2` | 2026-08-10 | 拿掉物理,改成等高階梯編排（= 第一個 commit,倉庫的起點） |
| `v0.3` | 2026-08-10 | 飛行規則定案：拋物線下降 + 只在命中時上升 |
| `v0.4` | 2026-08-10 | 離線／正式雙版本、觸艦滑行結局、單檔離線版 |
| `v0.5` | 2026-08-11 | 自動下注、甲板邊緣搖晃結局 |
| `v0.6` | 2026-08-11 | 所有物件 Prefab 化,預設美術抽成獨立元件 |
| `v0.7` | 2026-08-12 | 音效欄位（只接線,還沒有音檔） |
| `v0.8` | 2026-08-19 | 下方 bar 改版：下注步進器、速度選單、自動面板與自訂局數 |
| （併入 v1.0） | 2026-08-19 | v0.9 GameObject 搬進場景,版面在編輯器裡拖 —— 跟 v1.0 同一個 commit,沒有自己的 tag |
| `v1.0` | 2026-08-19 | 降落固定成功（滑行停在跑道上）、按鈕改深底半透明 |

常用指令：

```bash
git tag -n9                  # 列出全部版本 + 說明
git show v0.5                # 看那一版收尾 commit 的完整內容
git diff v0.5 v0.6           # 兩版之間差了什麼
git checkout v0.4            # 回到那一版的當時狀態（回來：git checkout main）
```

> `git checkout v0.4` 會進入 detached HEAD,那是正常的 —— 只是去看,別在上面改東西。
> 要回來就 `git checkout main`。

**下一版怎麼標**：改完 → 在下面「版本歷程」最上面加一段 → 這張表加一列 → commit →
`git tag -a v1.1 -m "v1.1 一句話"`。tag 打在那一版最後一個 commit 上。

---

## 版本歷程

### v1.0 — 2026-08-19 · 降落固定成功、按鈕改深底

**降落不再有「差點掉出去」**

碰到甲板 = 直接成功,滑行減速停在跑道上;沒碰到 = 落海。就這兩種。

兩個搖晃結局（`EDGE_HOLD` 穩住 / `EDGE_TIP` 翻落）的權重預設改成 **0**。
程式整段都還在,想把懸念加回來就把 Inspector ⑧ 的權重調成非 0 ——
演算法會照樣為了湊出那個結局去安排幾何。

降落的實際軌跡（`demo` 局的最後 16 幀,甲板 x = 4240～4544）：

```
tick 106  x=4240.0  觸艦（正好近端邊緣）
tick 107  x=4287.2  ┐
tick 108  x=4321.1  │ easeOutCubic 減速滑行
tick 109  x=4343.8  │
tick 110  x=4357.6  │
tick 111  x=4364.6  ┘
tick 112  x=4367.2  停住 —— 跑道 42% 處,傾角 0°,y = 甲板高度
```

停的位置由 `deckStopMin` / `deckStopMax`（④,預設 0.35～0.70）在甲板長度上隨機取,
想每次都停同一點就把兩個設成一樣的值。

**成功的收尾表演**：一圈往外擴的金色光暈 + 目的艦被壓一下 + 倍數彈一下。
**沒有加煙** —— v0.6 已經刻意拿掉「停穩之後再噴一次煙」,那看起來像憑空冒出來。

**按鈕底色改深色半透明**

原本是白色 12%。天空是亮藍的,白底疊上去等於沒有底,字浮在背景上很難讀。
改成深藍黑 65%（`rgba(6,16,30,.65)`）—— 看得出是一顆按鈕,但還是透得出後面的畫面。
按不動的狀態也從 alpha 26（幾乎消失）提到 92,現在看得出「有這顆但按不動」。

**驗證**：4000 局結局分佈 = `DECK_LAND 1965 / SPLASH 2035`,**兩種搖晃結局 0 次**;
`verify.ts` 8800+3000 局 0 失敗;降落最後 16 幀逐幀確認（x 收斂停住、傾角 0°、高度 = 甲板）。
按鈕新底色在離線版用瀏覽器看過。

---

### v0.9 — 2026-08-19 · GameObject 搬進場景,版面在編輯器裡拖

**問題**：所有 GameObject 都是程式 `new Node()` 出來的,位置寫死在 `AviaGame.buildUi()`
裡（例如 `W - 150, 74`）。要調一顆按鈕得改程式、存檔、等編譯、再預覽 ——
而且**沒有編輯器的人（我）根本無法確認擺得對不對**,v0.8 的驗證欄就卡在這裡。

**做法**：節點樹先寫進場景,程式改成「**場景裡有同名節點就用它,沒有才自己建**」。

| 你在編輯器做的事 | 結果 |
|---|---|
| 拖節點 | 改位置。程式不會蓋掉（`AviaView.place()` 只對自己建的節點生效） |
| 改 UITransform 寬高 | 改按鈕大小,程式照著畫（外框、圓角都跟著） |
| 刪掉某個節點 | 退回程式建,一切照舊 —— **場景空的也照樣跑得起來** |
| 在節點上掛自己的 Sprite | 用你的圖 |

`node tools/genlayout.js --write` 把整棵樹（60 個 GameObject）寫進 `game.scene`,
包含 `stage / sky / sun / camRoot / hud / ui` 與 UI 底下 40 幾個控制項。
會先備份到 `local/`（在 git 忽略清單裡,也不會被 Cocos 當成資產匯入),
而且**結構檢查沒過就不寫檔**。

**沒有搬進場景的東西**（位置由航線演算法決定,擺了也會被程式蓋掉）：
飛機、起飛航母、目的艦、`+N`／`×N`／飛彈、尾煙、特效。
起飛航母的位置是航線起點算出來的,照原本的作法生成 —— 要換外觀走 Inspector ⑨ 的 Prefab。

**編輯器裡看得到**（不然搬到場景也沒意義）

節點本身是空的,外觀是執行期用 Graphics 畫的 —— 編輯模式下沒人跑那段程式,
Scene 視窗裡只會看到一堆看不見的節點,位置還是得靠猜。

所以新增 `AviaUiArt.ts`：一顆 `@executeInEditMode` 的元件,把外框／底板／天空／太陽
**在編輯器裡畫出來**;文字則是節點自己的 `cc.Label`,場景裡存著,編輯器直接看得到字。
`genlayout.js` 會把這兩樣一起寫進場景（52 個預覽元件 + 44 個 Label）。

- **執行期與編輯器共用同一支 `drawUiBox()`** —— 編輯器看到的長相跟跑起來一模一樣,
  不會有兩套畫法各畫各的。
- **這顆元件只在編輯器動作**（`EDITOR` 判斷）。不然按鈕被打開的那一刻 `onEnable`
  會用「沒選中」的樣子重畫,把程式剛設好的選中狀態蓋掉。
- 執行期的 Label **沿用節點自己那顆**,不再另生 `label` 子節點 ——
  漏掉這步會變成「場景的字」跟「程式生的字」兩份疊在一起。

**開場那三個 bug（你回報的）**

| 症狀 | 原因 | 修法 |
|---|---|---|
| 開場景就噴 `Can't add component 'cc.Graphics' to spin because it conflicts with the existing 'cc.Label'` | **Cocos 一個節點只能有一個 UIRenderer** —— `Graphics` 與 `Label` 互斥。我把文字直接放在按鈕節點上,外框就畫不上去了 | 文字一律移到 `label` 子節點;`AviaUiArt` 遇到同節點有 Label 會警告並跳過,不再硬加 |
| 被選中的速度／AUTO 文字消失 | 同上 —— 那顆節點的 Graphics 加不上去,渲染只剩一個元件,字就被吃掉 | 同上。修好之後選中態是「黃底 + 深藍字」,字一直看得到 |
| 金額門檻只能在級距上點循環 | 原設計刻意避開輸入框 | 改成玩家自己輸入（見下） |

**停止條件金額改成自己輸入**

三條金額條件（單次獎金 / 餘額增加 / 餘額減少）右邊各有一個輸入框,**打多少就是多少**,
0 或空白 = 這條關閉。上限 `stopAmountMax`（預設 999999）。

- 語意變了：以前是「下注額的倍數」會隨下注額等比縮放,現在是**絕對金額**
- `stopAmountSteps` 這個 Inspector 參數已經移除（沒有級距可循環了),換成 `stopAmountMax`
- Cocos 版沿用同一顆數字鍵盤,靠 `kpTarget` 決定數字要寫進哪一格;離線版用 `<input type=number>`
- 兩邊都是「打進去會超過就不吃」,貼上 `9999999` 會變 `999999`,非數字直接清空

**順手做的**

- **太陽從天空的 Graphics 抽成獨立節點** —— 以前它是畫在天空色帶上的兩個圓,
  現在是真的 GameObject,可以拖、可以縮放、可以換成自己的圖。
  視差改成相對「你擺的位置」偏移,不是絕對座標。
- **螢幕震動改成相對 stage 的基準位置**,不然在場景裡把 stage 搬過位置會被每 frame 打回原點。
- **浮層節點在場景裡預設 `active: false`** —— 不然開場會閃一格才被 `refreshUi()` 關掉。
  打開時會重畫一次外框（`UiButton.repaint()`),不用去賭「關著時畫的東西打開還在不在」。
- **新增 `node tools/genlayout.js --check`** —— 對帳程式會找的節點名字與場景實際有的,
  這是沒有編輯器時唯一能抓出「改了程式忘了改場景」的辦法。目前 23/23 對得上。

**驗證**：場景 JSON 結構全檢（父子對稱、元件回指、必要欄位、孤兒節點）0 問題;
節點名對帳 23/23;演算法 8800+3000 局 0 失敗。
**畫面實際長相仍然要你開編輯器看** —— 但從這一版之後,不對就直接用滑鼠拖,不用回來改程式。

---

### v0.8 — 2026-08-19 · 下方 bar 改版：下注步進器、速度選單、自動面板

**下注：籌碼列 → `−  金額  ＋` 步進器**

原本 9 顆籌碼一字排開佔掉半條 bar,現在收成一個步進器。
**兩顆鍵都會循環** —— 在最小額按 − 跳到最大,在最大額按 ＋ 回到最小,
所以永遠不會有「按到底沒反應」的死角,也不用管清單有幾個選項。

**速度：四顆並排 → 選單**

一顆鍵顯示目前速度,點下去**往上展開**四個選項,選完就收起來。
往上是因為這顆鍵已經貼在畫面底部,往下展開會被切掉。
速度選單與自動面板**互斥**,開一個會關掉另一個 —— 兩個浮層疊在一起會打架。

**自動下注面板：**

- Cocos 版終於有真正的底板（新的 `AviaView.createFrame()`),不再是一堆按鈕浮在畫面上
- 分成「局數」與「停止條件」兩個小節,不再全部擠成一片按鈕
- **局數多了「自訂」** —— 玩家自己填數字,上限 `autoCountMax`（預設 99,也就是兩位數）
- 沒填數字就不讓他按「開始自動」,按鈕會直接變灰,不會按下去沒反應
- 「開始自動」按鈕上直接顯示局數（`開始自動  12 局` / `開始自動  ∞`）

自訂輸入在兩個版本用不同做法,理由不一樣：

| 版本 | 做法 | 為什麼 |
|---|---|---|
| 離線 HTML | `<input type=number>` | 瀏覽器原生,手機會自動跳數字鍵盤 |
| Cocos | 自己拼的數字鍵盤（1-9 / ⌫ 0 ✓） | Cocos 的 `EditBox` 需要 Sprite 背景圖,而這專案是零素材全向量繪製 |

兩邊的上限都用**「打進去會超過就不吃」**處理,不是打完再夾 —— 玩家不會看到數字先跳出來又被改掉。
貼上 `999` 會變成 `99`,貼上 `0` 或非數字會清空並鎖住開始鍵。

**驗證**：離線版用瀏覽器實跑過（下注循環雙向、速度選單展開/選取/收合、
自訂局數 1234→12、貼 999→99、貼 0→鎖住、自訂 3 局跑起來會倒數、Console 無錯誤）。
**Cocos 版的版面沒辦法在這裡驗** —— 座標與版面要你開編輯器看。

---

### v0.7 — 2026-08-12 · 音效欄位（只接線,還沒有音檔）

**目標是「以後要掛音效的時候不用改程式」**,所以這一版只做欄位與觸發點,一個音檔都沒有。
全部留空的狀態下遊戲行為跟上一版完全一樣。

新檔案 `AviaAudio.ts`,結構跟 `AviaArt.ts` 同一個路數 —— 沒掛素材也能跑。

| 東西 | 內容 |
|---|---|
| Inspector ⑩ 音效 | 19 格 AudioClip（16 顆一次性 + 3 條循環）+ 5 個音量 + 靜音 + 節流間隔 |
| 觸發點 | 全部 10 種 `BeatType` 都接上了,另外加上按鈕／改注／開局／自動下注開始結束 |
| 循環音 | `engine` 起飛開、降落／落海／回待機關;`ambience` 與 `bgm` 開場就播 |

設計上的幾個決定：

- **表演層只認 `AudioHooks`（`play` / `loop` 兩個方法）**,不認得 `AudioSource`。
  以後要換成第三方音訊引擎,換掉 `AviaAudio` 的實作就好,`AviaView` 一行都不用動。
- **沒指定音效層時用 `SILENT_AUDIO`**（兩個空函式）,所以 `AviaView` 裡到處都是
  `this.sfx.play(...)`,沒有一個 null 判斷。
- **音效綁動作,不綁按鈕**。SPIN 鍵不掛按鈕音 —— 它可能是「開一局」也可能是「停止自動」,
  而且餘額不足時按不動。所以 `spin` 音是在 `spin()` 通過餘額檢查之後才響的。
- **`nearMiss` 會連發**（誘餌很密）。音量自動壓到 55%,再加一道 `sfxMinGapMs`（預設 45ms）
  的節流,同一顆音效在間隔內只留第一下。
- **靜音不停播放,只把音量算成 0**。取消靜音就直接接回去,循環音不會從頭播。
- 音檔可以一格一格補,開場 Console 會印「音效 n/19 已掛載,未掛：…」。

**順手修掉的一個地雷**：`AVIA_DEFAULTS` 原本把 `Prefab` 欄位也收進去（值是 `null`),
所以「還原預設值」會把拖好的 Prefab 全部清空 —— 那不是還原,那是刪資料。
現在 `gendefaults.js` 會跳過 `Prefab` / `AudioClip` 這類資產參考,
150 個參數裡有 125 個可還原,25 個資產欄位**還原時不碰**。

離線版（`AviaOffline.html`）沒有音效 —— 它是零相依的單一 HTML,沒有地方放音檔。

---

### v0.6 — 2026-08-11 · 所有物件 Prefab 化,預設美術抽成獨立元件

（這一版當時沒寫紀錄,補記）

預設美術從 `AviaView.ts` 抽成 `AviaArt.ts`,並做成 `assets/prefabs/` 底下六個 Prefab
（Plane / Carrier / CarrierDest / Pickup / Boost / Rocket）。
Inspector ⑨ 每一種物件一格,**留空 = 用預設向量美術,指定 = 整個換掉**,程式不用動。

兩個約定：節點原點 = 物件中心（航母例外,原點 = 水線）;
`+N` / `×N` 的數字放一個叫 `value` 的 Label 子節點就會自動帶入,沒有也不會出錯。

另外：降落成功時不再噴最後那道煙 —— 觸艦（`DECK_TOUCH`）那一下已經冒過了,
飛機都停穩了再噴一次會像憑空冒出來。

---

### v0.5 — 2026-08-11 · 自動下注、甲板邊緣搖晃結局

**新表演：甲板邊緣搖晃**

觸艦 → 減速滑行 → **半截機身懸在甲板外** → 搖晃傾斜 → 兩種結果：

| 結果 | 表演 |
|---|---|
| `EDGE_HOLD` | 搖晃後穩住,傾斜停在邊緣 → 降落成功 |
| `EDGE_TIP` | 搖晃後前傾,翻落海中 → 降落失敗 |
| `SPLASH` | 根本沒碰到船,直接砸進海裡 |

**兩種結果的前半段完全共用** —— 玩家在搖晃結束前不知道會是哪一種。這是整局張力的最後一個高點,
也取代了 v0.4 的 `SLIDE_OFF`（那個一觸艦就注定要掉,沒有懸念）。

這個表演需要飛機能「停下來」,而原本 `x = tick × PX_PER_TICK` 讓它永遠停不下來。
所以航線從單軌（y）改成 **三軌（x, y, pitch）**：
- `Frame` 加 `x`,滑行段 x 用 easeOutCubic 減速收斂到甲板尾端
- `ps[]` 讓收尾段可以指定明確的傾角（搖晃／翻落不是由 dy 導出的）
- `carrierTick` 換成 `carrierX`（世界座標）,目的艦擺在「觸艦點 = 近端邊緣」的位置,
  所以一定會滑完整個甲板長度才停在尾端

新參數（Inspector ④）：`slideDecelTicks` `wobbleTicks` `wobbleAmpDeg` `wobbleCycles`
`settleTicks` `holdPitchDeg` `tipTicks` `tipPitchDeg` `minSlideRoom` `edgeOverhang`

**自動下注**

- 局數選項由 `autoCountOptions` 決定,有幾個就出現幾顆按鈕,**最後一個固定是無限循環**
- 開啟後下注額鎖死,**只剩速度能調**
- 四個停止條件可同時開：任何勝利／單次獎金 ≥ X／餘額增加 ≥ X／餘額減少 ≥ X
- X 沒有用文字輸入框 —— 在 `stopAmountSteps` 級距上點擊循環,**單位是「當前下注額的倍數」**,
  所以改下注額時門檻會等比縮放。手機好按,也沒有輸入驗證問題
- 連線失敗（正式版）會停止自動,不會無限重試

**修正**

- `sampleFrame()` 漏了內插新加的 `x` → HUD 顯示「距離 NaN」、捲動整個爆掉。
  瀏覽器實跑才抓到的,單元驗證看不出來（它直接讀 `frames[]` 不走取樣）
- 兩次被 `String.replace()` 的 `$'` 特殊序列咬到（取代字串裡有 `'$' + money(...)`,
  `$'` 會插入「符合處之後的全部內容」→ 檔案後半段被複製一份）。
  **教訓：用程式改檔案時一律用函式版 replacer `s.replace(a, () => b)`。**

---

### v0.4 — 2026-08-10 · 離線／正式雙版本、觸艦滑行結局、單檔離線版

| 改動 | 理由 |
|---|---|
| `AviaGame` 加 `offlineMode` / `serverUrl` / `requestTimeout` | 「離線與正式版做進遊戲中」。兩個版本唯一的差別就是 `fetchResult()` 從哪拿結果,其餘全部共用。連線失敗會**退款**,不會偷偷用本地結果頂替 |
| 新結局 `SLIDE_OFF`：該墜海的位置剛好有航母 → 觸艦、滑行、從甲板尾端翻落 | 「要是在原本該落海的時候停在航母上,那要滑行然後落入海中」。實測 30.5% 的落海局會走這條 |
| 海面航母佈局搬進 `AviaPath`（`SEA` / `seaCarrierX` / `carrierDeckAt`） | 上面那條規則要成立,演算法必須知道船在哪。渲染層改用同一份函式,兩邊位置保證一致 |
| 新增 `AviaOffline.html` 單檔離線版 + `tools/genoffline.js` | 「build 可以直接離線執行的版本」。雙擊就玩,不用伺服器/Cocos/網路 |
| 新增 `tools/serve.js`（零相依靜態伺服器） | Cocos 的 web build 用 ES module + fetch,`file://` 會被 CORS 擋,一定要走 HTTP |

**單檔離線版的關鍵設計**：演算法不是複製一份,而是由 `tools/genoffline.js` 從
`assets/scripts/AviaPath.ts` **原檔**用 `node:module` 的 `stripTypeScriptTypes()` 剝掉型別後注入。
所以航線邏輯永遠只有一份,改完 `AviaPath.ts` 重跑產生器就同步了 —— 不會出現
「Cocos 版和離線版行為不一樣」。只有渲染層（`tools/offline/renderer.js`,Canvas2D）是另寫的。

**實機驗證**（這是第一次真的看到畫面跑）：用瀏覽器開起來截圖確認,
航母平台、飛機、物件、飛彈右→左、HUD 實際距離/高度、鏡頭跟隨、彈道弧線全部正常,零 console 錯誤。
過程抓到一個 Canvas2D 版專有的 bug：尾煙存螢幕座標又每 frame 手動位移,誤差累積成一條斜線。
改存世界座標後正常（Cocos 版沒這問題,它的軌跡本來就在 world 節點底下）。

---

### v0.3 — 2026-08-10 · 飛行規則定案（拋物線下降 + 只在命中時上升）

| 改動 | 理由 |
|---|---|
| 段落形狀改成「命中時上升 → 拋物線下降」,拿掉 sag（下垂再爬升） | 飛行規則 1a/1b/1c。sag 會在非命中時段造成上升,違反「只有吃到 +N/×N 才會有上升的表演」 |
| 曲線從 Catmull-Rom 樣條改成**逐段解析式計算** | 樣條的 overshoot 會製造意外的上升。解析式段落（easeOut 爬升 + `peak − drop·s²` 拋物線）可以逐 tick 精確控制,不可能 overshoot |
| 新增 `GLIDE_DROP`(22) 與 `RISE_TICKS`(4),`STEP_UP/DOWN` 34 → 46 | 每個物件的淨變化 = ±46 − 22,仍然全局一致 |
| 新增 `riseWindows` 並在驗證工具斷言 | 「除了起飛爬升與命中表演,航線任何時刻都必須下降」現在是自動守住的硬規則 |
| `TICK_MS` 全面加快（medium 120 → 82ms） | 「平時飛行速度提升」 |
| 航母甲板抬高到 `DECK_Y`,船身從水線一路撐到甲板 | 「起飛要從航母起飛」+「航母的平台要比海平面提高到可見的範圍」。之前飛機浮在甲板上方 130px |
| 飛彈改成從畫面右側往左飛（`rocketApproach`,機首朝左 + 尾焰） | 「飛彈要從畫面右側往畫面左側飛行」。在自己的 tick 剛好抵達飛機的 x,所以命中時機不變 |
| 移除背景雲朵 | 「背景不要做雲朵」 |
| 場景產生器搬進 `tools/genscene.js`,uuid 改成沿用既有 meta | 之前每次重生都換 uuid,害 Cocos 重新匯入。現在冪等 |

**為什麼要換掉樣條**：v0.2 用 Catmull-Rom 串關鍵影格,曲線平滑但會在控制點附近 overshoot,
造成「沒吃到東西卻微微上升」。改成逐段解析式之後,每一 tick 的值都是直接算出來的,
規則變成由構造保證,而不是靠事後檢查。

---

### v0.2 — 2026-08-10 · 拿掉物理,改成等高階梯編排

**這是一次架構級改動。** 起因是「飛行軌跡不要使用物理,以表演為主」。

| 改動 | 理由 |
|---|---|
| 刪掉重力 / 速度積分 / 終端速度,改成「關鍵影格 + Catmull-Rom 取樣」 | 物理會跑飛、會貼天花板、會意外墜海,而且參數之間互相耦合難調。編排版的曲線形狀完全由參數決定 |
| 所有 `+N` / `×N` 一律抬 `STEP_UP`,火箭一律降 `STEP_DOWN` | 「每一個上升與下降都是等高的,不論數字與位置」。順帶讓航線變成可預測的階梯,調參直覺得多 |
| 從 `Style` 拿掉 `lift`（升降幅度的每局隨機縮放） | 有它就不是「永遠等高」了。每局隨機只剩「間距」與「下垂」 |
| 移除高度上限,加入**垂直鏡頭跟隨** | 「沒有天空限制,可以超過天空」+「攝影機也要跟上去」。海面/物件/特效掛進 `camRoot` 一起移動;天空隨高度往深藍染色並浮出星點 |
| 局長上限改成 `max(MAX_ROUND_TICKS, 最緊排列所需長度)` | 「不要讓路徑變成無限,一定要有終點」。這樣上限一定達得到,Inspector 怎麼調都不會產生無限航線 |
| 誘餌改成幾何淨空硬檢查（檢查整個佔位寬度而非單一 tick） | 「非計算路徑的數字都不能被碰到」。驗證工具會逐局斷言淨空 > 碰撞半徑 |
| 升降幅度大幅調小（衝量 34/52/56/−70 → 階距 34） | 「吃到加減號的升降幅度大幅度減少」 |
| `maxObjects` 14 → 30,`boostChance` 0.42 → 0.30,新增 `pickupStepTarget` | 「中途吃到的加減號的數量增加」。少用乘算、多用小額加值 → 序列變長 |
| `decoyDensity` 0.15 → 0.35 | 「路徑外隨機生成無關緊要的加減數字」 |
| `pxPerTick` 48 → 40 | 物件變多之後要讓同屏能看到更多個 |
| 驗證工具新增三項斷言：終點存在且在上限內、階距全等、誘餌淨空 | 這三個是新的硬規則,必須自動守住 |

**下垂量的收斂設計**（這段值得記住）：
```ts
const room = Math.min(popY, nextY) - FLOOR_Y;
const sag  = Math.min(SAG * style.sag, room * 0.55);
```
`0.55 < 1` → 曲線在數學上不可能穿過 `FLOOR_Y`。所以 v0.1 那些保險絲全部可以刪掉。

---

### v0.1 — 2026-08-10 · 初版（物理積分版,已淘汰）

第一版用 semi-implicit Euler 積分：每 tick `vy -= g; y += vy`,命中物件給向上衝量。
物件間距由「自由落到安全底線還剩幾 tick」× `tension` 決定。

**驗證過程抓到一個真 bug 並修掉**（保留紀錄,因為它說明了為什麼要換掉物理）：

> 火箭連擊時,`MIN_GAP` 的下限箝制會蓋過安全預算,飛機在半途觸水。
> 8800 局中發生 13 次。修法是三層：下限不得超過預算本身、依連續火箭數放大保留高度、
> 加上「還有物件沒吃時貼水滑行」的硬保證。

修完雖然 0 失敗,但這正是物理方案的問題 —— **安全性靠一層層補丁堆出來,而不是由構造保證**。
v0.2 的編排方案讓同樣的保證變成一行 `Math.min(..., room * 0.55)`。

---

## 設計決策紀錄（為什麼是這樣）

這幾條是整個專案的地基,改動前請先讀：

1. **結果先決（outcome-first）。** Server 只給 `{ multiplier, landed }`,客戶端反推一條
   「演起來剛好等於這個結果」的航線。**不要**反過來讓演出決定結果 —— 那樣結果不可控,
   而且上線後過不了認證。

2. **反向分解只有一個技巧：只有火箭能製造小數。**
   正向 `+k` `×m` `÷2`,逆向 `−k` `÷m` `×2`。從最終倍數倒推回 1,遇到小數就必須用火箭逆運算。
   所以 `0.5×` 必然含奇數顆火箭 —— 這讓「降落成功但賠 0.5 倍」在敘事上完全自洽。

3. **算術用 double 就精確。** 起點 1,運算只有加整數 / 乘整數 / 除以 2 →
   所有可達值都是 `k/2^j`（二進位有理數）→ 浮點完全精確,不需要定點數。
   這個性質**只在 `rocketDivisor = 2` 時成立**。

4. **誘餌不是裝飾,是可信度的地基。** 如果場上每個物件都被吃到,玩家三局就會發現路徑是假的。
   誘餌讓「命中」看起來像有機率性。`decoyDensity` 設 0 遊戲會立刻變得很假。

5. **`AviaView` 不准認識「倍數」「結果」。** 它只消費 `PerformanceScript`。
   這條界線守住了,換美術/換引擎/換 3D 都只要重寫 `AviaView`。

6. **速度只改播放 tick 的毫秒數,絕不碰數值。** 這是「結果先決」架構的直接證據,
   也是四段速度可以存在的原因。

7. **沒有 cash out → 整局腳本可以一次下發。** 就算玩家把結果挖出來也無法改變任何事
   （錢已經下了,沒有按鈕可按）。所以不用 WebSocket、不用逐 tick 推送、斷線重整可完美續播。
   **如果哪天要加 cash out,這條紅利就沒了,協定必須整個重做。**

---

## 下一步候選（還沒做）

- [x] ~~音效層~~ —— v0.7 已接好欄位與觸發點,**只差音檔**（丟進 `assets/` 拖進 Inspector ⑩ 就會響）
- [ ] 手機直式版面（目前 UI 是為 1280×720 橫式寫死座標,要改成錨點布局）
- [ ] 物件用 Spine / Sprite 取代 Graphics 向量繪製 —— 通道已經開好（Inspector ⑨),差素材
- [ ] Aviamasters 2 的四個 booster（磁鐵 / 雷射 / 氮氣 / 救生圈）
- [ ] 接真 server（換掉 `AviaPath.ts` 最底下的 `offlineResult()`）
- [x] ~~版本控制~~ —— 已經有 git 了,每一版都有 commit
