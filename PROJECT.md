# Don't Miss This Airplane — 專案說明

**類型**　單局制飛行博弈玩法原型（Aviamasters 類型）
**引擎**　Cocos Creator 3.8.6 / TypeScript
**規模**　約 7,600 行，零第三方執行期相依，零美術素材（全向量程式繪製）
**線上試玩**　<https://youqirf.github.io/Dont-miss-this-airplane/>
**原始碼**　<https://github.com/YouQiRF/Dont-miss-this-airplane>

---

## 一、專案定位

玩家下注後，一架飛機從航空母艦起飛，沿途撞擊 `+N`（加值）、`×N`（倍率）、飛彈（除以 2）
三種符號累積倍數，最後降落在目的艦上結算，或是墜海歸零。

這個專案的重點不在「做一款遊戲」，而在解一個**業界標準的線上博弈架構問題**：

> 伺服器基於 RNG 與 RTP 模型先算出結果，只回傳 `{ multiplier, landed }`。
> 客戶端必須反推一條「演起來剛好等於這個結果」的航線，
> 而且要讓玩家看不出結果是先決定的。

因此整個客戶端是一套**結果先決（outcome-first）的演出合成器**，
而不是一套物理模擬 —— 這是線上博弈與一般動作遊戲最根本的架構差異。

---

## 二、核心演算法

`assets/scripts/AviaPath.ts`（1,035 行）—— 不 import 任何 `cc` 模組，可單獨在 Node 執行與測試。

```
① 以 roundId 為 seed 開啟確定性亂數流       → 重整後重播完全一致
② 反向分解：從目標倍數逆推回 1
      正向  +k    ×m    ÷2（飛彈）
      逆向  −k    ÷m    ×2
   只有飛彈能製造小數 → 0.5× 必然包含奇數顆飛彈
③ 編排等高階梯：第 i 個物件的高度 = 第 i−1 個 ± 一個固定階距
④ 補關鍵影格：命中點 → 彈起 → 段落下垂 → 下一個命中點
⑤ Catmull-Rom 取樣成逐 tick 曲線，俯仰角取曲線斜率
⑥ 收尾：降落曲線落在目的艦甲板，或墜海曲線砸入水中
⑦ 灑誘餌：場上大量無關的加減符號，並以幾何淨空保證飛機碰不到
```

### 四個設計決策

**1. 不用物理引擎。**
沒有重力、沒有速度積分、沒有終端速度。每一段是解析式拋物線，形狀完全由參數決定。
好處是路徑可預先完整計算、可逐格驗證、不會因為浮點累積而「跑飛」。

**2. 數值完全精確，不需要定點數。**
起點為 1，可用運算只有「加整數 / 乘整數 / 除以 2」，
所以所有可達值都是 `k / 2^j` 形式的二進位有理數 —— IEEE 754 double 對這類值完全精確。
（僅在把除數改成非 2 的值時才會失準，此時程式會自動退回近似值並發出警告。）

**3. 不墜海是數學保證，不是修補出來的。**
段落下垂量會依「離水面還剩多少空間」自動收斂：

```ts
const room = Math.min(popY, nextY) - FLOOR_Y;
const sag  = Math.min(SAG * style.sag, room * 0.55);
```

因為 `0.55 < 1`，曲線在數學上不可能穿過水面。不需要任何重試迴圈或保險絲。

**4. 每一階等高。**
所有 `+N` / `×N` 一律升 `stepUp`，所有飛彈一律降 `stepDown`，
與數值大小、當前高度都無關。視覺節奏因此保持一致，
玩家無法從「這階跳得特別高」反推出接下來會拿到什麼。

### 結局張力設計

觸艦之後的演出，成功與失敗**完全共用同一段表演**：
滑行減速 → 半截機身懸在甲板外 → 開始搖晃傾斜。

玩家在搖晃結束前無法判斷結果。四種結局：

| 結局 | 表現 | 結果 |
|---|---|---|
| `DECK_LAND` | 乾淨停在甲板上 | 成功 |
| `EDGE_HOLD` | 衝到邊緣半懸空，搖晃後穩住 | 成功 |
| `EDGE_TIP` | 搖晃後前傾，翻落海中 | 失敗 |
| `SPLASH` | 未觸艦，直接砸入海面 | 失敗 |

抽中權重由 Inspector 控制，可依營運需求調整緊張感的分佈。

---

## 三、架構分層

| 模組 | 行數 | 職責 | 依賴 |
|---|---:|---|---|
| `AviaPath.ts` | 1,035 | 航線合成演算法：結果 → 物件序列 → 曲線 → 演出節拍 | 無 |
| `AviaGame.ts` | 1,364 | 組件本體：Inspector 參數、下注、局流程、輸入 | 全部 |
| `AviaView.ts` | 1,014 | 畫面表演：海浪、雲、天空、鏡頭、飛機、尾煙、特效、HUD | `cc` |
| `AviaArt.ts` | 273 | 預設向量美術，同時也是六個 Prefab 的內容 | `cc` |
| `AviaAudio.ts` | 202 | 音效層：事件 → 聲音，19 格 AudioClip | `cc` |
| `AviaUiArt.ts` | 197 | UI 外觀與編輯器即時預覽（`@executeInEditMode`） | `cc` |
| `AviaDefaults.ts` | 147 | 151 個參數的預設值單一來源 | 無 |

**關鍵的一條界線**：`AviaView` 完全不認識「倍數」與「結果」，它只消費 `PerformanceScript`。

因此要換成 3D、換成 Spine 骨架、換成完全不同的美術風格，
只需要重寫 `AviaView`，演算法一行都不用動。

美術與音效再往外分一層：外觀走 Prefab、聲音走 AudioClip，
兩者都是**沒指定就用內建的、指定了就整個換掉**，不需要改程式。
目前專案沒有掛任何音檔，而**沒有音檔也完全跑得動** —— 未掛載的事件單純靜音。

### 資料驅動的版面

場景樹底下的每個 UI 節點都可以在編輯器裡直接拖動、縮放、替換成自己的 Sprite。
程式的規則只有一條：**場景裡有同名節點就用它，沒有才由程式建立**。
場景整個清空也照樣能跑。

---

## 四、工程驗證

`tools/verify.ts` —— 不需要開啟 Cocos，純 Node 執行。

**樣本：8,800 局降落（22 個倍數 × 400 seed）＋ 3,000 局落海。結果：0 失敗。**

逐局檢查項目：

- 演出結果精確等於目標倍數，物件鏈正向重算一致
- 每個命中物件確實落在航線上
- 飛行途中不觸水
- 誘餌與航線的距離 > 碰撞半徑（整個佔位寬度都檢查，不是只檢查單一 tick）
- 航線有限、有終點、在上限之內，影格長度與終點相符
- 每一階的高度差都等於同一個常數
- 墜海位置的分佈不集中在最後一格

統計輸出：航程 38–300 tick（平均 124.8）、命中物件平均 7.6 個、誘餌平均 21.9 個。
同時列印 ASCII 航線圖供肉眼檢查等高階梯的形狀。

---

## 五、線上化路徑

`AviaPath.ts` 底部的 `offlineResult()` 換成一次 HTTP request 即可，其餘程式碼不需更動：

```ts
const result = await fetch('/api/spin', { ... }).then(r => r.json());
// → { roundId, multiplier, landed }
const script = buildPerformance(result);
```

因為這個玩法**沒有 cash out**（下注後沒有任何可按的按鈕），
整局腳本可以一次性下發給客戶端 —— 即使玩家把結果從記憶體挖出來也無法改變任何事。

這是砍掉 cash out 換來的架構紅利：
不需要 WebSocket、不需要逐 tick 推送、斷線重整可以完美續播。

---

## 六、工具鏈

| 指令 | 用途 |
|---|---|
| `node tools/verify.ts` | 11,800 局演算法驗證 |
| `node tools/genscene.js` | 產生場景 |
| `node tools/genlayout.js` | 產生／對帳 UI 版面節點樹（`--check` 比對程式與場景） |
| `node tools/genprefabs.js` | 產生六個物件 Prefab |
| `node tools/gendefaults.js` | 由程式碼反向產生參數文件 `DEFAULTS.md` |
| `node tools/genoffline.js` | 產生零相依單檔離線版 |
| `node tools/serve.js` | 零相依靜態伺服器 |

### 單檔離線版

`Dont_miss_this_airplane.html` —— 93 KB，單一檔案，**零外部請求**，雙擊即可遊玩。

演算法是在建置時直接從 `AviaPath.ts` 原始碼剝除型別後注入的，
所以航線邏輯永遠只有一份事實來源，
不會出現「Cocos 版和離線版行為不一致」這種難以追查的問題。
同一份內容同時輸出到 `docs/index.html` 作為 GitHub Pages 入口。

---

## 七、可調參數

Inspector 共 11 組、151 個參數，完整清單見 `DEFAULTS.md`（由程式碼自動產生）。

涵蓋下注選項、符號數值表、結果機率模型（`winChance` / `multiplierPool` / `biasPower`）、
航線編排（階距、密度、局長上限、誘餌淨空）、手感隨機範圍、播放速度、
畫面與鏡頭、結局權重、Prefab 替換、音效與音量，以及測試用的強制結果與航線除錯繪製。

**測試方式**：開啟 `forceResult`、將 `forceMultiplier` 設為 250，連按 SPIN ——
每次都會長出完全不同的航線，但結果一定是 250×。這是「結果先決」最直觀的驗證。
再指定 `forceSeed`，就會每次重現同一條航線。
